# TEST REPORT — <Tên project> — Học viên: ___________

> File này nộp cùng PR code, đặt tại `docs/test_report.md`. Điền đúng theo danh sách Test Case của project tương ứng (`TESTCASES_Px_student.md`). Học viên **tự chạy** toàn bộ test case trên máy/thiết bị của mình.

**Ngày chạy test:** ___________
**Môi trường chạy test:** (VD: Raspberry Pi Zero W, Yocto build ngày ___, hoặc: Ubuntu 22.04 VM)

---

## Kết quả Test

| Test ID | Kết quả (Pass/Fail/N-A) | Mô tả kết quả quan sát được |
|---|---|---|
| TC-XX-01 | | (mô tả ngắn gọn kết quả) |
| TC-XX-02 | | |
| ... | | |

> Với mỗi case **Pass**: ghi mô tả cụ thể (không chung chung "hoạt động đúng") — SSID cụ thể, nội dung text, giờ:phút:giây, etc.
> Với mỗi case **Fail**: ghi rõ nguyên nhân đã xác định được (hoặc "chưa xác định được nguyên nhân" nếu thật sự chưa tìm ra).
> Với mỗi case **N/A**: ghi rõ lý do không áp dụng được.

## Vấn đề đã biết nhưng chưa fix (nếu có)

Liệt kê trung thực các lỗi/hạn chế đã biết nhưng chưa kịp sửa trước khi nộp.

```
(1-3 gạch đầu dòng, hoặc ghi "không có")
```

## Tổng kết tự đánh giá

Số case Pass: ___ / Tổng số case: ___

```
(1-2 câu tự nhận xét mức độ hoàn thành so với yêu cầu)
```